# 视频学习归档

这里存放外部视频链接的学习资料，不进入日常视频剪辑流程。

默认流程：

1. 用户给出值得学习的视频链接。
2. 使用 `video-learning-archive` Skill 调用 `yt-dlp` 下载视频、元数据和字幕。
3. 在单独文件夹里生成逐字稿、总结和学习笔记。

入口命令：

```bash
npm run learn:video -- "URL"
```

如果 `yt-dlp` 未安装，脚本会提示安装命令；不要在未确认前自动安装。
