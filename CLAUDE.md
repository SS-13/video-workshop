# Claude Code Entry

Read and follow [AGENTS.md](AGENTS.md) as the single source of workflow rules.
Then run `npm run setup`, `npm run context`, and `npm run doctor`.

Do not create a separate Claude-specific workflow. Local personal content and
runtime state remain protected by `.gitignore`.
